/**
 * 
 * @param {*} test 
 * @param {*} name 
 * @param {*} variable 
 * @returns a test object
 */
function testFunction(test, name, variable) {
	return {
		test: test,
		name: name,
		variable: variable,
	}
}